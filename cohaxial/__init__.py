"""Small shared helpers for the COHAXIAL research scripts.

The repository intentionally keeps its runnable workflows as plain scripts at
the repository root.  This package holds only reusable support code that would
otherwise be duplicated across those scripts.
"""

